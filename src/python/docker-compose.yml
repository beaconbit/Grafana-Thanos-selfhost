#services:
#  python-app:
#    build:
#      context: .  # The directory containing your Dockerfile and application code
#      dockerfile: Dockerfile.python
#    environment:
#      - APP_ENV=production  # Example: Pass environment variables to the container
#    network_mode: host
#    cap_add:
#      - NET_RAW
#      - NET_ADMIN
#    command: ["python", "new_main.py"]
